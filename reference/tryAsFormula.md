# Turn character vector into formula, return error if not possible

Turn character vector into formula, return error if not possible

## Usage

``` r
tryAsFormula(input)
```

## Arguments

- input:

  character

## Value

Formula or error
